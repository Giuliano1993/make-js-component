const angularWizard = (componentName, folder) => {
    return {
        componentName,
        folder,
        framework: "angular",
        template: "component.component.js",
    };
};
export default angularWizard;
